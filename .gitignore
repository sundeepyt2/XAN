
# (it's in package.json but only for reference)
# .zscripts (dev environment)
# Caddyfile (deployment config)
# Cloudflare cookie storage (per-user, not committed)
# IDE
# OS
# agent-ctx (dev context)
# debug
# dependencies
# examples (websocket demo, not part of core app)
# local env files
# logs
# misc
# next.js
# prisma (not used in this project)
# production
# scripts (keep them, they're useful)
# scripts/
# skills (development environment, not part of the project)
# tool-results (temp)
# typescript
# upload (user-provided source files)
# vercel
# z-ai-web-dev-sdk (not used)
*.log
*.pem
*.swo
*.swp
*.tsbuildinfo
.DS_Store
.DS_Store?
.Spotlight-V100
.Trashes
._*
.cf-cookie.json
.env
.env*.local
.env.local
.idea/
.pnp.*
.pnpm-debug.log*
.vercel
.vscode/
/.next/
/.pnp
/.zscripts/
/Caddyfile
/agent-ctx/
/build
/examples/
/node_modules
/out/
/prisma/
/skills/
/tool-results/
/upload/
Thumbs.db
cf-worker/node_modules/
dev.log
ehthumbs.db
next-env.d.ts
npm-debug.log*
server.log
yarn-debug.log*
yarn-error.log*
