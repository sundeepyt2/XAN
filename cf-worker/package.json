{
  "name": "xan-stream-proxy",
  "version": "5.0.0",
  "private": true,
  "description": "XAN Cloudflare Worker — stream proxy + AllAnime episode resolver using direct crypto (no browser needed, free, no card, no external service)",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
