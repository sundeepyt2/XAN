{
  "name": "xan-stream-proxy",
  "version": "4.0.0",
  "private": true,
  "description": "XAN Cloudflare Worker — stream proxy + AllAnime episode resolver using Browser Rendering (free, no card, no external solver service)",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
